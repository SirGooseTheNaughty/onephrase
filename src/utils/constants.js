export const CUSTOM_CATEGORY_ID = window.CUSTOM_CATEGORY_ID || '710715095531';
export const MAX_LENGTH = window.MAX_LENGTH || 29;
export const MIN_LENGTH = window.MIN_LENGTH || 1;
export const TOO_SHORT_MSG = window.TOO_SHORT_MSG || 'Надпись не может быть пустой';
export const TOO_LONG_MSG = window.TOO_LONG_MSG || 'Надпись не может быть длиннее 29 символов';